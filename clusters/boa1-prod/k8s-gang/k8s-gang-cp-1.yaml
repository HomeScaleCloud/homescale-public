apiVersion: cdi.kubevirt.io/v1beta1
kind: DataVolume
metadata:
  name: k8s-gang-cp-1-boot
  namespace: k8s-gang
spec:
  source:
    http:
      url: "https://cloud-images.ubuntu.com/releases/noble/release/ubuntu-24.04-server-cloudimg-amd64.img"
  pvc:
    accessModes:
      - ReadWriteMany
    storageClassName: longhorn
    resources:
      requests:
        storage: 10Gi
---
apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: k8s-gang-cp-1
  namespace: k8s-gang
spec:
  runStrategy: Always
  template:
    metadata:
      labels:
        kubevirt.io/domain: k8s-gang-cp-1
    spec:
      domain:
        cpu:
          cores: 2
        resources:
          requests:
            memory: 2Gi
          limits:
            memory: 2Gi
        devices:
          disks:
            - name: bootdisk
              disk:
                bus: virtio
            - name: cloudinitdisk
              disk:
                bus: virtio
            - name: tailscale-auth
              serial: tailscaleauth
          interfaces:
            - name: default
              macAddress: "52:54:00:10:00:01"
              masquerade: {}
            - name: k8s-gang-net
              macAddress: "52:54:00:10:01:01"
              bridge: {}
      networks:
        - name: default
          pod: {}
        - name: k8s-gang-net
          multus:
            networkName: vlan-k8s-gang
      volumes:
        - name: bootdisk
          dataVolume:
            name: k8s-gang-cp-1-boot
        - name: cloudinitdisk
          cloudInitNoCloud:
            userData: |
              #cloud-config
              hostname: k8s-gang-cp-1
              package_update: true
              packages:
                - curl
              bootcmd:
                - mkdir -p /mnt/tailscale-auth
                - >-
                  mount /dev/$(lsblk --nodeps -no name,serial | grep tailscaleauth | cut -f1 -d' ')
                  /mnt/tailscale-auth
              runcmd:
                - curl -fsSL https://tailscale.com/install.sh | sh
                - tailscale up --ssh --authkey=$(cat /mnt/tailscale-auth/authkey) --hostname=k8s-gang-cp-1
            networkData: |
              network:
                version: 2
                ethernets:
                  eth0:
                    match:
                      macaddress: "52:54:00:10:00:01"
                    set-name: eth0
                    dhcp4: true
                  eth1:
                    match:
                      macaddress: "52:54:00:10:01:01"
                    set-name: eth1
                    dhcp4: false
                    addresses:
                      - "10.2.71.10/24"
        - name: tailscale-auth
          secret:
            secretName: k8s-gang-tailscale
