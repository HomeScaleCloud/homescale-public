apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-lon1-core-tls
  namespace: argocd
spec:
  secretName: argocd-server-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - "xxx"
  commonName: "xxx"
