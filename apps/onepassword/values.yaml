connect:
  connect:
    serviceType: ClusterIP
    credentialsName: onepassword
    credentialsKey: connect-credentials
  operator:
    create: true
    token:
      name: onepassword
      key: operator-token