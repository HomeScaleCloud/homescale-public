defaultNamespace: metrics-legacy
name: metrics-legacy
helm:
  releaseName: metrics-legacy
  chart: ./

overrideTargets:
  - clusterSelector:
      matchLabels:
        REDACTED/metrics-legacy: enabled
