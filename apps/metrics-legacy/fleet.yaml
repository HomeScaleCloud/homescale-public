defaultNamespace: metrics-legacy
name: metrics-legacy
helm:
  releaseName: metrics-legacy
  chart: ./

overrideTargets:
  - clusterSelector:
      matchLabels:
        app.homescale.cloud/metrics-legacy: enabled
