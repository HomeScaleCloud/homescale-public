apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: job-operator
rules:
  - apiGroups: ["batch"]
    resources:
      - jobs
    verbs: ["create", "delete"]
  - apiGroups: ["batch"]
    resources:
      - cronjobs
    verbs: ["get"]
