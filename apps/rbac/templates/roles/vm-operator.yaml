apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: vm-operator
rules:
  # Power lifecycle and live-migration only — no create/delete/spec edits on
  # the VirtualMachine/VMI objects themselves; those stay with GitOps.
  - apiGroups: ["subresources.kubevirt.io"]
    resources:
      - virtualmachines/start
      - virtualmachines/stop
      - virtualmachines/restart
      - virtualmachines/migrate
      - virtualmachines/evacuate/cancel
    verbs: ["update"]
  - apiGroups: ["subresources.kubevirt.io"]
    resources:
      - virtualmachineinstances/pause
      - virtualmachineinstances/unpause
      - virtualmachineinstances/freeze
      - virtualmachineinstances/unfreeze
      - virtualmachineinstances/softreboot
      - virtualmachineinstances/reset
      - virtualmachineinstances/evacuate/cancel
    verbs: ["update"]
  - apiGroups: ["kubevirt.io"]
    resources:
      - virtualmachineinstancemigrations
    verbs: ["get", "delete", "create", "update", "patch", "list", "watch", "deletecollection"]
