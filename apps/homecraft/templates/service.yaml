apiVersion: v1
kind: Service
metadata:
  name: "mc"
  labels:
    xxx/name: "{{ .Release.Name }}"
spec:
  type: ClusterIP
  ports:
  - name: minecraft
    port: 25565
    targetPort: minecraft
    protocol: TCP
  selector:
    app: "{{ .Release.Name }}-minecraft"
