tailscale-operator:
  apiServerProxyConfig:
    mode: "true"
  proxyConfig:
    defaultTags: "tag:app"
  operatorConfig:
    # hostname: k8s-core
    defaultTags:
      - "tag:k8s-operator"
      - "tag:k8s-api"