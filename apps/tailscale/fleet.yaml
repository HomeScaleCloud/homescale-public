defaultNamespace: tailscale
name: tailscale
helm:
  releaseName: tailscale
  chart: ./
  values:
    tailscale-operator:
      proxyConfig:
        defaultTags: "tag:app,tag:env-${ get .ClusterLabels \"xxx/env\" }"
      operatorConfig:
        hostname: "ts-${ .ClusterName }-${ get .ClusterLabels \"xxx/region\" }"
        defaultTags:
          - "tag:k8s"
          - "tag:env-${ get .ClusterLabels \"xxx/env\" }"
      apiServerProxyConfig:
        allowImpersonation: "true"
