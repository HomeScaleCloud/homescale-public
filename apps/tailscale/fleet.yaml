defaultNamespace: tailscale
name: tailscale
helm:
  releaseName: tailscale
  chart: ./
  values:
    tailscale-operator:
      proxyConfig:
        defaultTags: "tag:app,tag:env-${ get .ClusterLabels \"xxx/env\" }"
      operatorConfig:
        hostname: "ts-${ .ClusterName }-${ get .ClusterLabels \"xxx/env\" }-${ get .ClusterLabels \"xxx/region\" }"
        defaultTags:
          - "tag:k8s"
          - "tag:k8s-${ get .ClusterLabels \"xxx/env\" }"
          - "tag:env-${ get .ClusterLabels \"xxx/env\" }"
      apiServerProxyConfig:
        allowImpersonation: "true"
targetCustomizations:
  - name: mgmt
    clusterSelector:
      matchLabels:
        xxx/env: mgmt
    helm:
      values:
        tailscale-operator:
          operatorConfig:
            hostname: "ts-mgmt"
