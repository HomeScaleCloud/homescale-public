defaultNamespace: ziti
name: ziti-controller-mgmt-1
helm:
  releaseName: ziti-controller-mgmt-1
  chart: ./
  values:
    ziti-controller:
      cluster:
        mode: cluster-init
        trustDomain: REDACTED
        nodeName: controller-mgmt-1
      clientApi:
        advertisedHost: REDACTED
        advertisedPort: 443
        service:
          type: LoadBalancer
          annotations:
            external-dns.alpha.kubernetes.io/hostname: REDACTED
      webBindingPki:
        enabled: true
        altServerCerts:
          - mode: certManager
            secretName: tls-ziti-controller-mgmt-1
            altDnsNames:
              - "REDACTED"
            issuerRef:
              group: cert-manager.io
              kind: ClusterIssuer
              name: letsencrypt
            mountPath: /etc/ziti/alt-server-cert

overrideTargets:
  - clusterSelector:
      matchLabels:
        REDACTED/env: mgmt

dependsOn:
  - name: trust-manager
    acceptedStates:
      - Ready
