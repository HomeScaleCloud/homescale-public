defaultNamespace: ziti
name: ziti-controller-init
helm:
  releaseName: ziti-controller-init
  chart: ./
  values:
    ziti-controller:
      cluster:
        mode: cluster-init
        trustDomain: REDACTED
        nodeName: controller-init
      clientApi:
        advertisedHost: REDACTED
        advertisedPort: 32175
        service:
          type: NodePort
          annotations:
            external-dns.alpha.kubernetes.io/hostname: REDACTED
            external-dns.alpha.kubernetes.io/ttl: "60"

overrideTargets:
  - clusterSelector:
      matchLabels:
        REDACTED/env: mgmt
