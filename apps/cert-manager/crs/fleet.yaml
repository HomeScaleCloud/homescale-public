defaultNamespace: cert-manager
name: cert-manager-crs
helm:
  releaseName: cert-manager-crs
  chart: ./

dependsOn:
  - name: onepassword
    acceptedStates:
      - Ready

overrideTargets:
  - clusterSelector:
      matchLabels:
        app.homescale.cloud/cert-manager: enabled
