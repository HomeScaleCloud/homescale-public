defaultNamespace: cert-manager
name: cert-manager-crs
helm:
  releaseName: cert-manager-crs
  chart: ./
overrideTargets:
  - clusterSelector:
      matchLabels:
        REDACTED/cert-manager: enabled
