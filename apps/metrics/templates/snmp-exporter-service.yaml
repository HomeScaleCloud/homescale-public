{{- if .Values.snmpExporter.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: snmp-exporter
  namespace: metrics
  labels:
    app: snmp-exporter
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 9116
      targetPort: http
  selector:
    app: snmp-exporter
{{- end }}
