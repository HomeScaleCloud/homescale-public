defaultNamespace: home-assistant
name: home-assistant
helm:
  releaseName: home-assistant
  chart: ./
  values:
    image:
      repository: ghcr.io/home-assistant/home-assistant
      tag: "2025.12.5"
    ingress:
      enabled: true
      className: "tailscale"
      host: "ha-${ .ClusterName }-${ REDACTED/region }.tempel-carp.ts.net"
      annotations:
        "tailscale.com/hostname": "ha-${ .ClusterName }-${ REDACTED/region }"
        "tailscale.com/tags": "tag:app-ha,tag:env-${ REDACTED/env }"
    pvc:
      size: 25Gi
    zigbee:
      local: true
      devicePath: "/dev/ttyUSB0"