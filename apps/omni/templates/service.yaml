apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: omni
spec:
  selector:
    app.kubernetes.io/instance: omni
    app.kubernetes.io/name: omni
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
    - port: 443
      targetPort: 8080
      protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: k8s
  namespace: omni
spec:
  selector:
    app.kubernetes.io/instance: omni
    app.kubernetes.io/name: omni
  ports:
    - port: 443
      targetPort: 8095
      protocol: TCP
