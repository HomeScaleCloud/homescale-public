apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: maxmorris-io
  namespace: maxmorris-io
spec:
  parentRefs:
  - name: netbird-public
    namespace: netbird
  rules:
  - backendRefs:
    - name: maxmorris-io
      port: 80
