apiVersion: resources.teleport.dev/v3
kind: TeleportGithubConnector
metadata:
  name: github
  namespace: teleport
spec:
  display: GitHub
  teams_to_roles:
    - organization: HomeScaleCloud
      team: admins
      roles:
        - admin
