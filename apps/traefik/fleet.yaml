defaultNamespace: traefik
name: traefik
helm:
  releaseName: traefik
  chart: ./
  takeOwnership: true
  force: true
  values:
    traefik:
      deployment:
        replicas: 2
      service:
        type: ClusterIP
