apiVersion: v1
kind: ServiceAccount
metadata:
  name: mariadb-password-init # pragma: allowlist secret
  namespace: {{ .Release.Namespace }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-6"
    "helm.sh/hook-delete-policy": before-hook-creation
