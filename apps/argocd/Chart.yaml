apiVersion: v2
name: argocd
description: ArgoCD self-managed via Helm — installed initially by the cluster provisioner, then managed here
version: 0.1.0
dependencies:
  - name: argo-cd
    version: "7.9.1"
    repository: https://argoproj.github.io/argo-helm
