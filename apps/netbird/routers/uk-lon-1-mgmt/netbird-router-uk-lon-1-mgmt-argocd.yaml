apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: netbird-router-uk-lon-1-mgmt
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: 'https://github.com/MorrisLAN/morrislan'
      targetRevision: main
      path: 'apps/netbird/routers/uk-lon-1-mgmt'
  destination:
    server: https://kubernetes.default.svc
    namespace: netbird
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
