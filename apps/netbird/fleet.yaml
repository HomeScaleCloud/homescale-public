defaultNamespace: netbird
name: netbird
helm:
  releaseName: netbird
  chart: ./
  helm:
    values:
      kubernetes-operator:
        cluster:
          name: ${ .ClusterName }-${ get .ClusterLabels \"REDACTED/region\" }
        netbirdAPI:
          keyFromSecret:
            name: op-netbird
            key: password

targetCustomizations:
  - name: mgmt
    clusterSelector:
      matchLabels:
        REDACTED/env: mgmt
    helm:
      values:
        kubernetes-operator:
          cluster:
            name: mgmt

dependsOn:
  - name: cert-manager
    acceptedStates:
      - Ready
  - name: onepassword
    acceptedStates:
      - Ready
