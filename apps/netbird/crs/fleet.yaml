defaultNamespace: netbird
name: netbird-crs
helm:
  releaseName: netbird-crs
  chart: ./
  values:
    cluster:
      name: ${ .ClusterName }
      region: ${ get .ClusterLabels \"REDACTED/region\" }

dependsOn:
  - name: netbird
    acceptedStates:
      - Ready
