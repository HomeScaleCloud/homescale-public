apiVersion: config.supervisor.pinniped.dev/v1alpha1
kind: FederationDomain
metadata:
  name: homescale
  namespace: pinniped
spec:
  issuer: https://REDACTED/issuer
  identityProviders:
    - displayName: Entra ID
      objectRef:
        apiGroup: idp.supervisor.pinniped.dev
        kind: OIDCIdentityProvider
        name: entra
