apiVersion: v1
kind: Service
metadata:
  name: pinniped-impersonation
  namespace: pinniped
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    app.kubernetes.io/name: pinniped
    app.kubernetes.io/component: concierge
  ports:
    - protocol: TCP
      port: 443
      targetPort: 8444
