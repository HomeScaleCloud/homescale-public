apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pinniped
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: 'https://github.com/HomeScaleCloud/homescale'
      targetRevision: main
      path: 'apps/pinniped/base/manifests'
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: pinniped
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=true
