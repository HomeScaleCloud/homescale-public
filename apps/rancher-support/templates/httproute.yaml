apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TLSRoute
metadata:
  name: rancher-proxy
  namespace: cattle-system
spec:
  hostnames:
    - REDACTED
  parentRefs:
    - name: netbird-public
      namespace: netbird
      sectionName: rancher-tls
  rules:
    - backendRefs:
        - name: rancher
          port: 443
