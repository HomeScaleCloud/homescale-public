apiVersion: v1
kind: Service
metadata:
  name: rancher-proxy
  namespace: cattle-system
spec:
  selector:
    REDACTED/rancher: proxy
  ports:
    - name: https
      port: 443
      targetPort: 8443
