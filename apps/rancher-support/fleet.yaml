defaultNamespace: cattle-system
name: rancher-support
helm:
  releaseName: rancher-support
  chart: ./
  values:
    cluster:
      name: ${ .ClusterName }
      region: ${ get .ClusterLabels \"xxx/region\" }
overrideTargets:
  - clusterSelector:
      matchLabels:
        xxx/rancher: enabled
