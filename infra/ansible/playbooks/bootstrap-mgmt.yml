---
- name: Bootstrap mgmt-class cluster
  hosts: localhost
  gather_facts: false

  vars:
    cluster_name: mgmt
    kubeconfig_secret_key: "{{ cluster_name | upper | replace('-', '_') }}_KUBECONFIG"
    kubeconfig_path: "/tmp/kubeconfig-{{ cluster_name }}"
    omni_cluster_name: "{{ cluster_name }}"

  module_defaults:
    kubernetes.core.k8s:
      kubeconfig: "{{ kubeconfig_path }}"
    kubernetes.core.k8s_info:
      kubeconfig: "{{ kubeconfig_path }}"
    kubernetes.core.helm:
      kubeconfig: "{{ kubeconfig_path }}"

  pre_tasks:
    # Automatron (the in-cluster runner) authenticates by reusing the k8s Infisical
    # Operator's own identity (INFISICAL_OPERATOR_CLIENT_ID/SECRET, mounted as env
    # vars) rather than getting a dedicated identity of its own — Infisical machine
    # identities are billed per-identity. `hsctl run` (local) has no such credential
    # to present, so it pre-fetches the same secrets itself via the caller's
    # already-logged-in `infisical` CLI and passes them in as hsctl_local_secrets,
    # skipping these two tasks entirely.
    - name: Login to Infisical
      infisical.vault.login:
        url: "{{ infisical_url }}"
        auth_method: universal_auth
        client_id: "{{ lookup('env', 'INFISICAL_OPERATOR_CLIENT_ID') }}"
        client_secret: "{{ lookup('env', 'INFISICAL_OPERATOR_CLIENT_SECRET') }}"
      register: _infisical
      no_log: true
      when: not (hsctl_local | default(false))

    - name: Read cluster kubeconfig from Infisical
      infisical.vault.read_secrets:
        login_data: "{{ _infisical.login_data }}"
        project_id: "{{ infisical_project_id }}"
        env_slug: "{{ infisical_env_slug }}"
        path: /k8s/automatron
        as_dict: true
      register: _mgmt_kubeconfig_secrets
      no_log: true
      when: not (hsctl_local | default(false))

    - name: Use cluster kubeconfig fetched locally via the infisical CLI
      ansible.builtin.set_fact:
        _mgmt_kubeconfig_secrets:
          secrets: "{{ hsctl_local_secrets.mgmt_kubeconfig }}"
      no_log: true
      when: hsctl_local | default(false)

    - name: Skip if kubeconfig not yet available
      when: >
        _mgmt_kubeconfig_secrets.secrets[kubeconfig_secret_key] is not defined
        or not _mgmt_kubeconfig_secrets.secrets[kubeconfig_secret_key]
      ansible.builtin.meta: end_play

    - name: Write kubeconfig
      ansible.builtin.copy:
        content: "{{ _mgmt_kubeconfig_secrets.secrets[kubeconfig_secret_key] }}"
        dest: "{{ kubeconfig_path }}"
        mode: "0600"
      no_log: true

    - name: Wait for k8s API
      kubernetes.core.k8s_info:
        api_version: v1
        kind: Namespace
        name: kube-system
      register: _api_probe
      retries: 6
      delay: 10
      until: _api_probe is not failed

  roles:
    - cluster-secrets
    - argocd

  post_tasks:
    - name: Remove temporary kubeconfig
      ansible.builtin.file:
        path: "{{ kubeconfig_path }}"
        state: absent
